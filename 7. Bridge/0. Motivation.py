"""
Connecting components together through abstractions

A mechanism that decouples(разъединять) an interface (hierarchy) from an implementation (hierarchy)
Both can exist as hierarchies
A stronger form of encapsulation

Bridge prevents a 'Cartesian product' ( дека́ртово произведе́ние двух множеств )
avoids the entity explosion
"""